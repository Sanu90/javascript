// check whether an array is palindrome or not //

// let array = [1, 2, 3, 10, 3, 2, 1];

// function palindrome(array, left = 0, right = array.length - 1) {
//   if (left >= right) {
//     return true;
//   }
//   if (array[left] != array[right]) {
//     return false;
//   }
//   //console.log("hi");
//   return palindrome(array, left + 1, right - 1);
// }

// console.log(palindrome(array));

// check whether a string is palindrome or not //

// let str = "malayaalam";

// function palindrome(str, left = 0, right = str.length - 1) {
//   if (left >= right) return true;
//   if (str[left] != str[right]) return false;
//   return palindrome(str, left + 1, right - 1);
// }

// console.log(palindrome(str));
