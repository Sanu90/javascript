// Array.of() creates a new instance of array with variable number of arguments //

const arr = Array.of(10, 5, 8, 3);
console.log(arr);

//Array.from() creates a new array instance from an array like or iterable instance. //

const arr1 = Array.from("123");
console.log(arr1);
